{
  "name": "fit-csv-converter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "fit-file-parser": "^4.1.0"
  },
  "devDependencies": {
    "vite": "^7.1.1",
    "wrangler": "^4.28.1"
  }
}
